# kosher_salt
A tool used to parse data from a form into a file template.

## Installation
Make sure to install packages and run servers locally for both "/client" and "/server". To run each server, use <code>yarn start</code>
